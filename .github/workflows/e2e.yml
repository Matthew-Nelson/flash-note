name: E2E Tests

on:
  # push:
  #   branches: [main, develop]
  #   paths:
  #     - 'web/**'
  #     - '.github/workflows/e2e.yml'
  # pull_request:
  #   branches: [main, develop]
  #   paths:
  #     - 'web/**'
  #     - '.github/workflows/e2e.yml'
  workflow_dispatch:

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '10'

jobs:
  # TODO: Rewrite for Next.js embedded database
  # web-e2e:
  #   name: Web E2E
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 30
  #
  #   services:
  #     postgres:
  #       image: postgres:16
  #       env:
  #         POSTGRES_USER: test
  #         POSTGRES_PASSWORD: test
  #         POSTGRES_DB: flashnote_test
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v2
  #       with:
  #         version: ${{ env.PNPM_VERSION }}
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'pnpm'
  #
  #     - name: Install dependencies
  #       run: pnpm install --frozen-lockfile
  #
  #     - name: Setup database
  #       run: pnpm db:migrate
  #       working-directory: ./web
  #       env:
  #         NODE_ENV: test
  #         DATABASE_URL: postgres://test:test@localhost:5432/flashnote_test
  #
  #     - name: Build Next.js app
  #       run: pnpm build
  #       working-directory: ./web
  #
  #     - name: Start Next.js server
  #       run: |
  #         pnpm start &
  #         echo $! > /tmp/web.pid
  #       working-directory: ./web
  #       env:
  #         NODE_ENV: test
  #         DATABASE_URL: postgres://test:test@localhost:5432/flashnote_test
  #
  #     - name: Wait for server
  #       run: |
  #         for i in {1..30}; do
  #           curl -sf http://localhost:3000 > /dev/null && echo "Server ready" && exit 0
  #           sleep 2
  #         done
  #         echo "Server failed to start" && exit 1
  #
  #     - name: Install Playwright browsers
  #       run: pnpm exec playwright install --with-deps chromium
  #       working-directory: ./web
  #
  #     - name: Run E2E tests
  #       run: pnpm test:e2e:ci --project=chromium
  #       working-directory: ./web
  #       env:
  #         CI: true
  #         WEB_URL: http://localhost:3000
  #
  #     - name: Upload Playwright report
  #       uses: actions/upload-artifact@v4
  #       if: ${{ !cancelled() }}
  #       with:
  #         name: web-playwright-report
  #         path: web/playwright-report/
  #         retention-days: 7
  #
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: ${{ !cancelled() }}
  #       with:
  #         name: web-test-results
  #         path: web/test-results/
  #         retention-days: 7
  #
  #     - name: Stop server
  #       if: always()
  #       run: kill $(cat /tmp/web.pid) 2>/dev/null || true

  placeholder:
    name: E2E (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E tests disabled — pending rewrite for Next.js embedded database architecture"

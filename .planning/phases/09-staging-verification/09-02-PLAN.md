---
phase: 09-staging-verification
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: false
requirements: [INFRA-10, INFRA-11, INFRA-12]

must_haves:
  truths:
    - "A user can register with an invite code, receive a verification email, verify their email, and log in"
    - "A logged-in user can generate a SOAP note from shorthand input via Vertex AI ADC"
    - "A user can complete Stripe checkout (test mode), webhook fires, subscription activates, and note generation is unlocked"
    - "A user can log out and their session is invalidated"
    - "Connection pool handles concurrent requests without errors"
  artifacts:
    - path: "staging.flashnote.co"
      provides: "Running application with all integrations verified"
    - path: "Stripe Dashboard (test mode)"
      provides: "Completed checkout session, processed webhook events"
    - path: "Cloud SQL staging database"
      provides: "User record, session record, subscription record, usage record"
  key_links:
    - from: "Registration form"
      to: "Cloud SQL users table"
      via: "Server Action -> DAL -> INSERT"
      pattern: "register.*invite_code"
    - from: "Note generation form"
      to: "Vertex AI (Gemini)"
      via: "Server Action -> LLM service -> gemini-provider.ts ADC fetch"
      pattern: "GEMINI_USE_ADC=true"
    - from: "Stripe checkout"
      to: "Webhook endpoint"
      via: "Stripe fires events to staging.flashnote.co/api/webhooks/stripe"
      pattern: "checkout.session.completed"
    - from: "Webhook handler"
      to: "Cloud SQL users table"
      via: "Billing service updates subscription_status"
      pattern: "subscription_status.*active"
---

<objective>
Execute end-to-end smoke tests for auth flow, note generation, and Stripe billing on the running staging instance.

Purpose: Verify all integrations work together in the deployed environment. The application is running (Plan 01), but no user flow has been tested end-to-end. This plan walks through the three remaining success criteria: full auth flow (INFRA-11), Vertex AI ADC note generation (INFRA-10), and Stripe billing (INFRA-12).

Output: All 4 phase success criteria verified. Phase 4 complete.
</objective>

<execution_context>
@/Users/matthewnelson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewnelson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staging-verification/04-CONTEXT.md
@.planning/phases/04-staging-verification/04-RESEARCH.md
@.planning/phases/04-staging-verification/04-01-SUMMARY.md

Key reference files for troubleshooting:
@web/src/server/db/config.ts (env validation)
@web/src/server/services/billing.ts (Stripe webhook handling, checkout flow)
@web/src/server/services/llm/gemini-provider.ts (ADC token fetch)
@web/src/actions/auth.ts (registration, login, logout actions)
@web/src/actions/notes.ts (note generation action)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Auth flow and note generation smoke test (INFRA-10, INFRA-11)</name>
  <action>
Walk through the full user flow on staging: register with invite code, verify email, log in, generate a SOAP note (verifying Vertex AI ADC), and log out. Insert the staging invite code first, then complete each step and report pass/fail. The note generation step implicitly verifies INFRA-10 (Vertex AI ADC) because the staging environment uses GEMINI_USE_ADC=true with the Vertex AI endpoint. If any step fails, report the exact error for diagnosis and fix.

**Prerequisite: Insert staging invite code**
Connect to the staging Cloud SQL database and run:
```sql
INSERT INTO invite_codes (code, type, max_uses, expires_at)
VALUES ('STAGING-TEST-2026', 'personal', 100, '2027-01-01T00:00:00Z');
```
You can connect via:
- `gcloud sql connect {instance-name} --database=flashnote --user=flashnote --project=flashnote-staging`
- Or Cloud SQL Studio in GCP Console

**Step 1: Register** -- Go to staging.flashnote.co/signup, fill in name, email (real inbox), password (8+ chars, upper, lower, number), invite code STAGING-TEST-2026, accept Terms+BAA, submit. Expected: redirect to /check-email.

**Step 2: Verify email** -- Check inbox for email from noreply@flashnote.app, click verification link. Expected: redirect to login page with verified message.

**Step 3: Log in** -- Go to staging.flashnote.co/login, enter credentials. Expected: redirect to /dashboard.

**Step 4: Generate note (INFRA-10)** -- Navigate to note generation, enter shorthand (e.g., "pt 45yo M c/o LBP x 3 weeks, worse w/ sitting. AROM: flex 50%, ext 75%. Rx: manual therapy L-spine, ther ex core stab. Pt tolerated well."). Click generate. Expected: structured SOAP note with S/O/A/P sections.

**Step 5: Log out** -- Click logout. Expected: redirect to login, cannot access /dashboard without re-authenticating.

**Step 6: Connection pool sanity** -- Log back in, generate 2-3 more notes quickly, check health endpoint (curl -s https://staging.flashnote.co/api/health), check Cloud Logging for pool/connection errors. Expected: no errors.
  </action>
  <verify>All 6 steps pass: registration, email verification, login, note generation, logout, and connection pool sanity check.</verify>
  <done>INFRA-10 verified (Vertex AI ADC works -- note generated via staging). INFRA-11 verified (full auth flow register->verify->login->generate->logout works end-to-end).</done>
  <resume-signal>Report pass/fail for each step. If any fail, paste the exact error message from the UI or Cloud Logging.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stripe billing smoke test (INFRA-12)</name>
  <action>
Walk through the Stripe billing flow on staging: initiate checkout, complete payment with test card, verify webhook processing, confirm subscription activation, and verify note generation is unlocked. The billing flow uses server-side Checkout Sessions (no client-side Stripe.js). Stripe fires webhook events to staging.flashnote.co/api/webhooks/stripe after payment. If any step fails, report the exact error for diagnosis and fix.

**Step 1: Start checkout** -- Log in to staging, navigate to pricing/upgrade, click subscribe to monthly plan. Expected: redirect to Stripe checkout page (checkout.stripe.com).

**Step 2: Complete payment** -- Use test card 4242 4242 4242 4242, expiry 12/30, CVC 123, any name/email. Click Pay. Expected: redirect to staging.flashnote.co/dashboard?success=true.

**Step 3: Verify webhook** -- Check Stripe Dashboard -> Developers -> Webhooks -> your staging endpoint. Look for checkout.session.completed and invoice.payment_succeeded events. Each should show 200 response. Check Cloud Logging for webhook processing logs (filter: "webhook" OR "stripe").

**Step 4: Verify subscription** -- Refresh dashboard. Subscription status should be active (trial banner gone). Optionally check database: SELECT email, subscription_status, stripe_customer_id FROM users WHERE email = 'YOUR_EMAIL'.

**Step 5: Verify notes unlocked** -- Generate another SOAP note. Expected: succeeds without subscription gate.

If checkout redirect fails: check Stripe secret key and price IDs in Secret Manager.
If webhook returns non-200: check Cloud Logging for WebhookSignatureError, verify stripe-webhook-secret matches Stripe Dashboard.
If subscription does not activate: check webhook delivery status in Stripe Dashboard, check billing service logs.
  </action>
  <verify>All 5 steps pass: checkout redirect, payment completion, webhook 200 response, subscription active in database, and note generation unlocked.</verify>
  <done>INFRA-12 verified: Stripe test-mode checkout completes, webhook fires and processes correctly, subscription activates, note generation unlocked.</done>
  <resume-signal>Report pass/fail for each step. If any fail, paste the exact error from Stripe Dashboard, Cloud Logging, or the staging UI.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete verification -- all 4 success criteria:
1. INFRA-09: Pino logs in Cloud Logging, errors in Error Reporting (Plan 01, Task 3)
2. INFRA-10: Note generation via Vertex AI ADC works (Plan 02, Task 1, Step 4)
3. INFRA-11: Full auth flow works end-to-end (Plan 02, Task 1, Steps 1-5)
4. INFRA-12: Stripe checkout, webhook, subscription, and feature unlock work (Plan 02, Task 2)
</verification>

<success_criteria>
1. A user can register with invite code, verify email, log in, generate a note, and log out on staging
2. Note generation uses Vertex AI ADC (not consumer API key) -- confirmed by successful generation with GEMINI_USE_ADC=true
3. Stripe test-mode checkout completes, webhook fires and returns 200, subscription activates in database
4. Active subscription unlocks note generation
5. No connection pool errors during multi-note generation
6. All issues discovered during verification are fixed and re-verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-verification/04-02-SUMMARY.md`
</output>
